2026-03-02 [docs/plans/2026-03-02-default-load-and-homepage-data.md] 新增规划文档：①各模块（macro/cashflow/summary/micro）进入时以最新一条历史记录作为默认加载项；②首页 Bento 栏目（WealthGauge、SavingsRateBar、HealthPills）使用最新 macro/cashflow/summary 数据渲染；含现状分析、设计要点、实施步骤与验收预期。
2026-03-02 [macro/cashflow/summary/micro/page.tsx, micro/actions+repo, micro/tools/*, app/page.tsx] 实施「默认加载最新记录 + 首页数据化」：① macro/cashflow 页面挂载时在 Supabase 已配置且存在记录的前提下自动调用 getLatestXXXAction，将最新一条填入表单并即时计算结果（仅执行一次，避免覆盖用户修改）；summary 页面挂载时自动执行原有「自动读取模块 1 与模块 2 最新记录」逻辑，并在 macro+cashflow 均存在时直接生成全景诊断结果；② micro 仓储新增 getLatestMicroCalculation，actions 新增 getLatestMicroCalcAction，/micro 页面在当前工具切换或首次进入时拉取该工具最新一条的 input_payload，通过 registry 声明的 toolType + 各工具组件新增的 initialInput props，将历史输入轻量回填；③ 首页 page.tsx 在服务端调用 getLatestMacroCheckAction/getLatestCashflowAction 获取最新 macro/cashflow 记录，WealthGauge/SavingsRateBar 改为接受 percentile/rate props 并在有数据时显示真实同龄人百分位与储蓄率，HealthPills 通过 calculateMacroResult + calculateCashFlow + generateSummary 得到净资产健康度、应急金充足度与高息负债标志，动态生成三枚药丸标签；Supabase 未配置或无记录时自动降级为当前静态占位内容；next build 通过。
2026-02-27 [lib/finance/ppp.ts] AGE_BRACKETS 基准数据按对数正态分布从 Fidelity 表中反推：各年龄段使用 Average Net Worth 与 Median Net Worth 计算 LogNormal(μ, σ²) 参数（μ = ln(median), σ = sqrt(2(ln(mean) - ln(median)))），再通过 Pq = exp(μ + Φ⁻¹(q)·σ) 计算 P25/P50/P75/P90；20s/30s/40s/50s/60s 使用原表 (mean, median)，70+ 沿用 60s；同龄人水位对比曲线与 macro 页所示 Fidelity 来源完全一致。
2026-02-27 [package.json] 本地开发脚本 dev 端口调整为 8112（next dev -p 8112），便于与其他服务区分与端口占用管理，生产启动脚本保持不变。
2026-02-27 [收入轨迹预测 + FIRE 达成路径] 新功能：①新增 lib/finance/income-projection.ts 纯函数模块，包含 yearlyIncomeGrowthRate（按年龄段返回职业成长率：22–50岁 10%→7%→5%→3.5%→2%→1%→0%）、projectIncomeAtAge（逐年复利推算指定年龄收入）、simulateFIREPath（逐年模拟净资产积累至目标，返回关键节点路径及达成年龄）；②macro/page.tsx 结果面板将原「职业成长模型」收入预测改为基于 Fidelity 官方表格的「收入成长轨迹（Fidelity 基准）」：新增 FIDELITY_INCOME_ROWS 静态数据（Age / Average Net Worth / Median Net Worth / Median Income / Recommended Net Worth / Income Multiplier），主表按 30/40/50/60/70 岁展示中位数收入、推荐净资产与收入倍数，并提供「查看数据来源表格」按钮展开原始表格，文案说明其与上方 PPP 同龄人水位表同源，仅作认知对齐；③cashflow/page.tsx：FormState 新增 currentNetWorth（当前净资产，可选）和 investmentReturn（投资年化收益率，默认6%）字段；currentAge / retirementAge 字段从「仅智能模式可见」升级为「表单始终可见」（智能模式仍需必填验证）；「自动读取模块01」一键导入同步填充净资产 net_worth；在「理想生活目标」区块顶部新增退休年龄+投资年化两格输入；useMemo 驱动 fireSimulation（simulateFIREPath 结果），当 result 存在且 currentAge 有效时自动激活；结果面板新增「FIRE 达成路径预测」区块：达成年龄大标题卡片（含是否早于退休的色彩判断）、里程碑路径表（预计收入/年度储蓄/累积净资产）、可视化进度条（今→达标→退休）；接口无破坏性变更，构建0错误。
2026-02-27 [cashflow 智能估算模式] ①lib/finance/cashflow.ts 新增 smartEstimateRetirementSpend 纯函数（E_ret = E_cur×R×(1+i)^n）、SmartEstimateParams/Result 接口、ReplacementLevel 枚举及 REPLACEMENT_PRESETS 三档预设（基础60%/维持80%/充裕100%）；②cashflow/page.tsx 理想生活目标区块新增「自定义输入 / ⚡智能估算」双模式 Toggle，智能模式下展示年龄输入、替代率三档按钮、通胀率微调框（默认2.5%）和逐行拆解的计算过程，结果只读输入框实时更新；目标开支值通过 useMemo 派生（不写回表单 state），handleCalc/handleSave 统一从 effectiveTargetSpend 取值；切换到智能模式时自动尝试从模块01读取年龄；「自动读取模块01」按钮升级为同时填充年收入与年龄；结果面板在智能模式下额外展示替代率/距退休年数/通胀系数三格汇总；接口无破坏性变更。
2026-02-27 [HistoryPanel 删除 + cashflow 导入模块01] ①三个 repository 新增 deleteMacroCheck/deleteCashflowAnalysis/deleteMicroCalculation；②三个 action 文件新增对应 delete action；③HistoryPanel 添加 onDelete/deletingId 可选 prop，每行末尾显示垃圾桶图标，首次点击变为红色"确认?"，2.5s 内再次点击才执行删除，防误操作；④cashflow 页面：在年收入字段上方新增"自动读取模块 01 最新年收入"按钮（需 Supabase 已配置），调用 getLatestMacroCheckAction 一键填充 annualIncome，有成功/无记录两种提示反馈；cashflow HistoryPanel 接入 onDelete；⑤macro 页面 HistoryPanel 同步接入 onDelete；接口变更：HistoryPanel props 新增 onDelete/deletingId（可选，不传则不显示删除按钮）。
2026-02-27 [全局去噪] 移除所有冗余标签与 CTA 文字：①page.tsx 删除四张模块卡右上角编号药丸（"模块 01/02/04"）与三张工具卡编号药丸（"T·A/B/C"），删除所有"进入模块→"/"打开工具→"底部 CTA 文字块，图标行布局由 justify-between 改为左对齐独占一行；②ui.tsx PageHeader 移除 tag 药丸渲染，tag/tagColor 改为 @deprecated 可选属性（保持向后兼容，不破坏现有调用）；③micro/page.tsx 移除 Tab 栏工具编号标签渲染及 Header"模块 03"药丸；④registry.ts 从 ToolDef 接口和所有注册表条目中删除 tag 字段；构建 0 错误。今后避免在卡片上放置编号药丸与重复的 CTA 文字——卡片本身的可点击性已由视觉设计（hover 效果、cursor）传达。
2026-02-27 [全局视觉重构] 深色优先 Premium Dark Mode 全面重构：①globals.css 完全重写——色彩系统翻转为深色优先（#090A0F 背景），新增 --primary/#00E5FF 品牌青蓝、--success/--warning/--danger 语义色、毛玻璃 .card-premium（backdrop-filter: blur(20px)）、发光输入框焦点、品牌色 .btn-primary、交错入场动画 @keyframes cardEnter、Bento 网格 .bento-grid/.bento-hero/.bento-tools 布局类、.num 等宽数字防抖动；②layout.tsx 新增 color-scheme meta；③新建 src/components/MagneticCard.tsx 磁性 3D 悬浮客户端组件（鼠标位置驱动 perspective rotateX/Y）；④page.tsx 全面改写为 Bento 网格首页，模块01大英雄卡（SVG 半圆仪表盘装饰）、模块02侧边卡（储蓄率进度条装饰）、模块04侧边卡（健康药丸状态装饰）、模块03三枚磁性小卡片；⑤components/ui.tsx 新增 AnimatedNumber（rAF 缓动数字滚动）、MiniGauge（SVG 仪表盘可视化）、MetricCard 升级为等宽数字；⑥micro/page.tsx Tab 活动项新增背景药丸与底部指示线发光效果；构建验证 0 错误 11 路由通过。外观行为变化：深色模式下毛玻璃卡片、背景渐变景深、全新品牌青蓝色交互，浅色模式通过 @media 覆盖保持可用。
2026-02-27 [src/app/micro/page.tsx, src/app/page.tsx] 修复微观工具路由：首页工具卡片链接由 /micro 改为 /micro?tool=xxx 携带工具 ID；micro/page.tsx 引入 useSearchParams+useRouter，从 URL 读取初始工具、Tab 切换时 router.replace 同步 URL（保留刷新状态）；useSearchParams 调用提取至 MicroContent 子组件并用 Suspense 包裹，满足 Next.js SSR 预渲染要求；API 无变化，构建通过。
2026-02-27 [docs/plans/2026-02-27-financial-health-system-plan.md] 新增全生命周期财务健康与微观投资测算系统的 Next.js + Supabase 技术方案规划文档，明确模块划分、数据模型与实施阶段。
2026-02-27 [docs/plans/2026-02-27-sprint-development-plan.md] 新增按 Sprint 1-4 拆分的开发步骤计划表，覆盖基础骨架、核心 UI、接口联调与配置打包阶段。
2026-02-27 [src/app/micro] 微观工具矩阵重构：三工具整合至 /micro 单页，新增 tools/ 目录（registry.ts 注册表 + DailyYield/PathXirr/NavReturn 三个独立工具组件），Tab 切换使用 CSS display 保留各工具表单状态，底部指示线动效，旧子路由改为服务端 redirect 保持兼容性；扩展时仅需添加注册记录和组件文件。
2026-02-27 [Sprint 3] 完成接口联调：新增 db/schema.sql（Supabase 建表+RLS），lib/supabase/server.ts+config.ts（服务端客户端+配置检测），repositories/macro+cashflow+micro（CRUD），actions/macro+cashflow+micro（Server Actions），components/HistoryPanel.tsx（折叠式历史记录组件）；更新 macro/cashflow/summary 页面集成保存按钮、历史加载与一键加载最近数据功能；支持 Supabase 未配置时优雅降级，仅改 .env.local 即可切换实例。
2026-02-27 [Sprint 2] 完成核心 UI：新增 src/lib/format.ts、src/components/ui.tsx 共享组件；实现全部 8 个页面（macro/cashflow/micro/micro/daily-yield/micro/nav-return/micro/path-xirr/summary）；globals.css 补充表单/按钮/指标卡片 CSS 类；所有页面均调用 lib/finance 纯函数计算，无 TypeScript 错误，9 个路由构建通过。
2026-02-27 [src/app/page.tsx, globals.css] 全局样式升级：引入 CSS 变量色彩令牌（深色/浅色双模式）、卡片圆角升级至 20px、弥散阴影与上浮悬停动效（translateY -6px）、图标区带彩色 accent 背景、副标题全大写加字间距、正文行高 1.65、进入模块箭头横移微动效，从「工具感」提升至「高级产品感」。
2026-02-27 [Sprint 1] 完成基础骨架：安装 @supabase/supabase-js、zod、vitest；创建 lib/supabase/client.ts；实现 lib/finance 下 7 个纯函数计算模块（ppp、lifecycle-targets、cashflow、micro-daily-yield、micro-nav-return、micro-path-xirr、summary）；编写 45 条单元测试全部通过；更新首页导航与 layout metadata；新增 .env.local 模板。
2026-04-22 [docs/plans/2026-04-22-okx-data-plan.md] 新增 OKX 交易复盘页实施规划，明确 CSV 结构、核心账户指标、结构分布、排行与行为标签等交付范围，并约定首页入口接入方式。
2026-04-22 [src/lib/finance/okx.ts, src/app/okx/page.tsx, src/app/okx/OkxDashboard.tsx, src/lib/finance/__tests__/okx.test.ts, src/app/page.tsx, src/app/globals.css] 新增 OKX 合约复盘室：服务端读取 public/static/okx__2025-04-20~2026-04-20.csv，完成交易解析、净盈亏/手续费/胜率/回撤聚合、币种/杠杆/持仓时长分布、行为标签、曲线与散点图展示，并把入口接入首页。
2026-04-22 [src/lib/finance/okx.ts, src/app/okx/OkxDashboard.tsx] 新增本金 USDT 输入与本地缓存：支持输入充值本金并持久化到 localStorage，派生真实资金曲线、本金回报率、最低资金位、最大回撤与是否跌破 0 的分析视角。
2026-04-22 [src/app/okx/OkxDashboard.tsx, docs/change-log.md] 优化 OKX 页面可读性：重做 FlowBars，去掉密集重叠数字，增加最佳/最差/最新窗口摘要；币种净盈亏排行改为“利润引擎 / 损耗黑洞”双面板，补充交易次数、胜率、手续费、单笔均值与贡献占比。
